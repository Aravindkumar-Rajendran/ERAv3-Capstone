meta {
  name: InteractTimeline
  type: http
  seq: 11
}

post {
  url: http://localhost:8000/interact-timeline
  body: multipartForm
  auth: bearer
}

auth:bearer {
  token: {{auth_token}}
}

body:multipart-form {
  conversation_id: {{conversation_id}}
  topics: Historical Events @contentType(Array)
  topics: Timeline Creation
} 